import type { TExpressApp } from '../types/TExpressApp';
/**
 * Initialize static folders
 * @param staticFolders
 * @param basePath
 * @param app @type {TExpressApp}
 */
export declare function initStaticFolders(staticFolders: string[], basePath: string, app: TExpressApp): void;
